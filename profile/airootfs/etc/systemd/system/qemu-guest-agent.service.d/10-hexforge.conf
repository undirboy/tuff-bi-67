[Unit]
# The unit is enabled unconditionally so the image works under QEMU/KVM out
# of the box; skip it cleanly everywhere else instead of failing at boot.
ConditionVirtualization=|kvm
ConditionVirtualization=|qemu
