# Compressed swap in RAM. On a 4 GB guest this is the difference between
# "Firefox and Metasploit at the same time" and the OOM killer.
#
# Handled by systemd-zram-generator; no service to enable.

[zram0]
zram-size = min(ram, 4096)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
